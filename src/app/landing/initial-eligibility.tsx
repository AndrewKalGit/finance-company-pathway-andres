// Main goal: instantly see if they are a fit with initial screening
// Maybe like a check box and at the end they can submit their email it pops up? instant feedback loop

const eligibility = () => {
	return <div></div>;
};

export default eligibility;
